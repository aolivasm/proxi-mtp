% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parametric.R
\name{pmtp_parametric_suite}
\alias{pmtp_parametric_suite}
\title{Fit the supplement's eight proximal parametric estimators}
\usage{
pmtp_parametric_suite(
  data,
  spec = pmtp_dgp_spec(),
  weights = NULL,
  population_size = NULL,
  start_h_correct = NULL,
  start_h_misspecified = NULL,
  start_g_correct = NULL,
  start_g_misspecified = NULL,
  max_iterations = 500L,
  misspecified_treatment_model = c("log_linear", "constant_v")
)
}
\arguments{
\item{data}{A data frame containing scalar columns \code{Y}, \code{A}, \code{L}, \code{Z}, and
\code{W}.}

\item{spec}{A specification created by \code{\link[=pmtp_dgp_spec]{pmtp_dgp_spec()}}. Only its known
policy parameters are used by the estimating equations.}

\item{weights}{Optional positive observation weights or the name of a column
in \code{data} containing them.}

\item{population_size}{Full phase-one population size. By default, uses the
sum of the analysis weights. Supplying the known population size is
recommended with inverse two-phase sampling weights.}

\item{start_h_correct, start_h_misspecified}{Optional starting values for the
correct and misspecified outcome bridge models.}

\item{start_g_correct, start_g_misspecified}{Optional starting values for the
correct and misspecified treatment bridge models.}

\item{max_iterations}{Maximum iterations for each numerical solver.}

\item{misspecified_treatment_model}{Treatment-bridge model used for the
deliberately misspecified estimators. The default \code{"log_linear"}
reproduces the original simulation code. \code{"constant_v"} retains the known
policy structure and provides a milder sensitivity analysis.}
}
\value{
An object of class \code{pmtp_parametric_suite} containing the eight
estimates, their joint covariance matrix and influence functions, the
four component fits, and the shared bridge solutions. The \code{solution_type}
component identifies exact roots and minimum-distance solutions.
}
\description{
Fits the correctly specified and deliberately misspecified outcome and
treatment bridge models from Supplement C.3. Each of the four bridge
functions is fitted once. The shared fits are then combined into two OR,
two DQW, and four DR estimators with stacked influence-function inference.
}
