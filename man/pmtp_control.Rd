% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{pmtp_control}
\alias{pmtp_control}
\title{Control tuning and numerical behavior for proximal MTP estimation}
\usage{
pmtp_control(
  outer_folds = 3L,
  inner_folds = 3L,
  tune = TRUE,
  lambda_h = 10^seq(-5, -1, by = 1),
  lambda_gp = 10^seq(-1, 2, by = 1),
  lambda_g = 10^seq(-5, -1, by = 1),
  lambda_hp = 10^seq(-1, 2, by = 1),
  bandwidth_h = c(1/4, 1/2, 1, 2, 4),
  bandwidth_gp = 1/4,
  bandwidth_g = c(1/4, 1/2, 1, 2, 4),
  bandwidth_hp = 1/4,
  risk_bandwidth = 1/4,
  risk_penalty = 1,
  critical_radius_rule = c("legacy_d1", "gaussian_dimension", "matern_sobolev"),
  max_norm_h = 50,
  max_norm_g = 50,
  seed = 1234L,
  jitter = 1e-08,
  max_solve_tries = 8L,
  keep_cv = TRUE,
  progress = FALSE,
  selection_rule = c("minimum", "one_se_regularized", "one_se_interior"),
  inner_repeats = 1L,
  kernel_approximation = c("exact", "nystrom"),
  nystrom_rank = pmtp_nystrom_rank(),
  nystrom_landmarks = c("uniform", "weighted"),
  cache_kernel_features = TRUE,
  kernel_family = c("gaussian", "matern_sobolev"),
  matern_smoothness = 2,
  sobolev_l = 4,
  weighted_loss_normalization = c("hajek", "horvitz_thompson")
)
}
\arguments{
\item{outer_folds}{Number of outer cross-fitting folds. Must be at least 2.}

\item{inner_folds}{Number of inner tuning folds within each outer-training
sample. Must be at least 2 when \code{tune = TRUE}; ignored otherwise.}

\item{tune}{Logical; perform inner cross-validation. When \code{FALSE}, every
tuning field must be scalar and those values are used directly.}

\item{lambda_h, lambda_g}{Scaling factors for the outer RKHS penalties for the
outcome and treatment bridges. The base rate is computed from the number
of observed rows in the relevant fold, including under two-phase sampling;
it is not computed from the sum of inverse-probability weights.}

\item{lambda_gp, lambda_hp}{Scaling factors for the inner adversarial RKHS
penalties for the outcome and treatment bridge objectives, using the same
observed-fold sample-size convention.}

\item{bandwidth_h, bandwidth_g}{Multipliers for the median-heuristic external
kernel variances.}

\item{bandwidth_gp, bandwidth_hp}{Multipliers for the median-heuristic
adversarial kernel variances.}

\item{risk_bandwidth}{Multiplier for validation-risk kernel variances. The
base bandwidth is recomputed on each validation fold.}

\item{risk_penalty}{Scaling factor for the squared critical-radius rate in
validation risks.}

\item{critical_radius_rule}{Penalty-rate rule. \code{"legacy_d1"} preserves the
preprint implementation, using \code{log(n)} as though every Gaussian RKHS had
dimension one. The \code{"gaussian_dimension"} rule used in the revised paper uses
\code{log(n)^d}, where \code{d} is the number of columns in the relevant bridge or
adversarial-kernel input. The number of observed rows in each fold remains
the sample-size argument. The \code{"matern_sobolev"} rule uses the polynomial
critical-radius rate determined by the input dimension and \code{sobolev_l}.}

\item{max_norm_h, max_norm_g}{Optional RKHS norm bounds. Use \code{Inf} to disable.}

\item{seed}{Random seed used for fold creation without permanently changing
the caller's random-number state.}

\item{jitter}{Initial scale-aware diagonal jitter for numerical solves.}

\item{max_solve_tries}{Maximum number of increasing-jitter Cholesky attempts.}

\item{keep_cv}{Logical; retain candidate-level cross-validation results.}

\item{progress}{Logical; print fold progress.}

\item{selection_rule}{Cross-validation selection rule. \code{"minimum"} selects
the smallest mean validation risk. The experimental
\code{"one_se_regularized"} rule forms a one-standard-error set using the
foldwise risk standard error at the minimum, then lexicographically
prefers larger outer and inner penalties followed by larger external and
adversarial kernel bandwidths; remaining ties use the smallest mean risk.
The experimental
\code{"one_se_interior"}
rule instead prefers the fewest boundary coordinates within the same
one-standard-error set, then uses the smallest mean risk.}

\item{inner_repeats}{Number of independently randomized inner-CV partitions.
The default of one reproduces ordinary inner cross-validation. Values
greater than one average validation risk over all repeat-by-fold
evaluations while preserving the training fraction of \code{inner_folds}.}

\item{kernel_approximation}{Kernel backend. \code{"exact"} uses the complete
fold-specific Gram matrices. \code{"nystrom"} uses fold-specific Nystrom
features for bridge fitting and validation risks.}

\item{nystrom_rank}{Positive fixed rank or a function of the number of rows
in the current fold. The default \code{\link[=pmtp_nystrom_rank]{pmtp_nystrom_rank()}} grows as
\code{2 * n^(2/3)}, subject to a minimum of 30 and the fold size.}

\item{nystrom_landmarks}{Landmark sampling scheme. \code{"uniform"} samples rows
uniformly; \code{"weighted"} samples in proportion to the analysis weights.}

\item{cache_kernel_features}{Logical; reuse fold-specific Nystrom feature
maps and shared minimax systems across tuning candidates with the same
bandwidths and inner penalty. This changes only computation, not
landmarks, risks, or fitted estimators. It is ignored by the exact
backend.}

\item{kernel_family}{RKHS kernel family. \code{"gaussian"} uses the Gaussian RBF
kernel. \code{"matern_sobolev"} uses a Matérn kernel whose native space is
equivalent to a Sobolev space.}

\item{matern_smoothness}{Positive Matérn smoothness parameter.}

\item{sobolev_l}{Positive spectral-order parameter used by the
\code{"matern_sobolev"} critical-radius rule. It equals twice
\code{matern_smoothness}.}

\item{weighted_loss_normalization}{Normalization for weighted empirical
bridge losses. \code{"hajek"} divides each fold-specific weighted sum by the
sum of its inverse-probability weights and preserves earlier analyses.
\code{"horvitz_thompson"}, used in the revised paper, divides by the
corresponding phase-one fold size, matching the estimator displayed in
the paper. The latter requires \code{population_size} in \code{\link[=pmtp]{pmtp()}} for an exact
full-sample normalization; fold sizes are allocated in proportion to the
number of observed phase-two rows in each split.}
}
\value{
A list of class \code{pmtp_control}.
}
\description{
The default grids reproduce the scaling-factor grids described in the
preprint. They can be expensive because every configuration is evaluated in
nested cross-validation. Use \code{\link[=pmtp_control_fast]{pmtp_control_fast()}} for examples and smoke
tests, but not as the final tuning strategy for a scientific analysis.
}
