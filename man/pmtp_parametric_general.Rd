% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parametric-general.R
\name{pmtp_parametric_general}
\alias{pmtp_parametric_general}
\title{Fit general parametric proximal MTP estimators}
\usage{
pmtp_parametric_general(
  data,
  treatment,
  outcome,
  covariates,
  negative_control_treatment,
  negative_control_outcome,
  policy,
  weights = NULL,
  target = NULL,
  policy_support = NULL,
  population_size = NULL,
  outcome_bridge = NULL,
  outcome_instruments = NULL,
  treatment_bridge = NULL,
  treatment_instruments = NULL,
  outcome_link = c("logit", "identity"),
  standardize = TRUE,
  log_g_bounds = c(-30, 30),
  start_h = NULL,
  start_g = NULL,
  max_iterations = 500L,
  conf_level = 0.95,
  strict = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{treatment, outcome}{Single treatment and outcome column names.}

\item{covariates}{Measured-covariate column names.}

\item{negative_control_treatment}{Negative-control treatment column names.}

\item{negative_control_outcome}{Negative-control outcome column names.}

\item{policy}{A policy function or named list of policy functions, with the
same interface as \code{\link[=pmtp]{pmtp()}}.}

\item{weights}{Optional positive inverse-inclusion-weight column name or
numeric vector. Do not normalize sampling weights to mean one.}

\item{target}{Optional target-population column name or 0/1 vector.}

\item{policy_support}{Optional support indicator or list of indicators. See
\code{\link[=pmtp]{pmtp()}}.}

\item{population_size}{Known phase-one population size. When omitted, uses
the sum of the supplied weights.}

\item{outcome_bridge}{Formula for the outcome bridge design
\code{h(A,L,W)}. The default contains treatment, squared treatment, measured
covariates, and negative-control outcomes.}

\item{outcome_instruments}{Formula for the outcome bridge moments. The
number of resulting columns must be at least the number of outcome-bridge
coefficients.}

\item{treatment_bridge}{Formula for the positive log-linear treatment
bridge \code{g(A,L,Z)}.}

\item{treatment_instruments}{Formula defining \code{v(A,L,W)} in the treatment
bridge moments. The number of resulting columns must be at least the
number of treatment-bridge coefficients.}

\item{outcome_link}{Outcome-bridge inverse link, either \code{"logit"} or
\code{"identity"}. A logistic link is convenient for binary outcomes but is a
working bridge restriction, not an ordinary outcome regression.}

\item{standardize}{Logical; standardize nonconstant model-matrix columns
using weighted means and standard deviations. The same transformation is
reused at policy-shifted treatment values. Overidentified systems require
\code{standardize = FALSE} so their minimum-distance metric is fixed.}

\item{log_g_bounds}{Two finite bounds used to prevent numerical overflow in
the log-linear treatment bridge. A positive \code{bound_fraction} diagnostic
indicates that the fitted model reached a bound.}

\item{start_h}{Optional outcome-bridge starting coefficients.}

\item{start_g}{Optional treatment-bridge starting coefficients, or a list
with one vector per policy.}

\item{max_iterations}{Maximum numerical-solver iterations.}

\item{conf_level}{Confidence level for reported intervals.}

\item{strict}{Logical; if \code{TRUE}, stop when any bridge has neither an exact
root nor a stationary minimum-distance solution. If \code{FALSE}, return point
estimates and convergence diagnostics with unavailable standard errors.}
}
\value{
An object of class \code{pmtp_parametric_general_fit} containing the
three estimators per policy, bridge coefficients and solution diagnostics,
model-matrix specifications, nuisance predictions, influence functions,
and the joint covariance matrix across policies and estimators.
}
\description{
Fits fully parametric proximal outcome and treatment bridge models for one
or more modified treatment policies. Unlike \code{\link[=pmtp_parametric]{pmtp_parametric()}}, this
function is not tied to the paper's simulation DGP: it accepts multiple
measured covariates, arbitrary monotone policies, formula-defined bridge
models and instruments, and inverse two-phase sampling weights.
}
\details{
The default outcome bridge is logistic and contains main effects of
treatment, measured covariates and negative-control outcomes, together with
a quadratic treatment term. Its default instruments replace the
negative-control outcomes by the negative-control treatments. The default
treatment bridge is positive and log-linear in treatment, measured
covariates and negative-control treatments. Its moment instruments replace
the negative-control treatments by the negative-control outcomes.

For every policy, the treatment bridge solves moments of the form
\code{P[target * v(q(A), L, W) - support * g(A,L,Z) * v(A,L,W)] = 0}.
The OR, DQW and DR estimators are divided by the weighted target-population
mass. Standard errors use the stacked influence function and squared inverse
sampling weights. Exact empirical roots are used when they exist; otherwise
a stationary minimum-distance solution is used and labeled explicitly.
}
