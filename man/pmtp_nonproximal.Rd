% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonproximal.R
\name{pmtp_nonproximal}
\alias{pmtp_nonproximal}
\title{Estimate a non-proximal modified-treatment-policy mean}
\usage{
pmtp_nonproximal(
  data,
  treatment = "A",
  outcome = "Y",
  covariates = c("L", "Z", "W"),
  policy,
  weights = NULL,
  target = NULL,
  population_size = NULL,
  estimators = c("sdr", "tmle"),
  engine = c("auto", "lmtp", "weighted_point"),
  folds = 5L,
  learner_folds = 10L,
  learners_outcome = NULL,
  learners_treatment = NULL,
  probability_bounds = c(0.001, 0.999),
  seed = 20260722L,
  return_fits = FALSE
)
}
\arguments{
\item{data}{A data frame.}

\item{treatment, outcome}{Single column names.}

\item{covariates}{Adjustment-variable column names.}

\item{policy}{A vectorized function of treatment, or an \code{lmtp}-style
function of \code{data} and the treatment column name.}

\item{weights}{Optional sampling-weight column name or numeric vector.}

\item{target}{Optional target-population column name or 0/1 vector. All
complete rows are retained for nuisance estimation. The indicator
restricts the shifted target distribution and final estimand.}

\item{population_size}{Full phase-one population size. Defaults to the sum
of the complete-case analysis weights.}

\item{estimators}{Any subset of \code{c("sdr", "tmle")}.}

\item{engine}{\code{"auto"}, \code{"lmtp"}, or \code{"weighted_point"}. Auto uses \code{lmtp}
only for unweighted, unrestricted targets and the audited point-treatment
engine otherwise. The \code{lmtp} engine cannot implement a restricted target
while fitting nuisances on the full data.}

\item{folds}{Number of outer cross-fitting folds.}

\item{learner_folds}{Number of internal SuperLearner folds.}

\item{learners_outcome, learners_treatment}{SuperLearner libraries. With
\code{NULL}, the exact paper library is used for \code{lmtp} and the weight-aware
paper library is used for \code{weighted_point}.}

\item{probability_bounds}{Bounds applied to the stacked-classification
probabilities before conversion to density ratios.}

\item{seed}{Random seed.}

\item{return_fits}{Whether to retain potentially large SuperLearner fits for
the weighted engine.}
}
\value{
An object of class \code{pmtp_nonproximal_fit}.
}
\description{
Provides the AIPW/SDR and TMLE comparators used in the paper. Unweighted
analyses can call \code{lmtp} directly. For inverse-probability weighted
two-phase analyses, \code{engine = "weighted_point"} uses a point-treatment
implementation that passes sampling weights to both the outcome and stacked
density-ratio SuperLearners, the TMLE fluctuation, the final empirical mean,
and the influence-function variance.
}
