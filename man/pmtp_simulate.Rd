% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{pmtp_simulate}
\alias{pmtp_simulate}
\title{Reproduce one simulation setting}
\usage{
pmtp_simulate(
  n,
  beta_z = 2,
  beta_w = -2,
  scenario = c("main", "c6", "c7", "c8", "c9"),
  estimator = c("gaussian", "sobolev", "nonproximal", "parametric"),
  weighted = FALSE,
  replications = seq_len(500),
  seed = NULL
)
}
\arguments{
\item{n}{Sample size, or target phase-two sample size when \code{weighted = TRUE}.}

\item{beta_z, beta_w}{Proxy coefficients in \code{\link[=pmtp_dgp_spec]{pmtp_dgp_spec()}}.}

\item{scenario}{Data-generating scenario; see \code{\link[=pmtp_paper_scenario]{pmtp_paper_scenario()}}.}

\item{estimator}{Estimator family. \code{nonproximal} returns AIPW and TMLE;
\code{parametric} returns the parametric bridge estimators.}

\item{weighted}{Use the two-phase sampling design with cohort size \code{15 * n}.}

\item{replications}{Positive integer replication IDs. Subsets can be run
separately without changing the random seeds for any replication.}

\item{seed}{Base seed. Defaults are 20280722 for primary/weighted Gaussian
simulations, 20260722 for secondary Gaussian simulations, 20290722 for
Sobolev, 20260750 for non-proximal, and 20300723 for parametric simulations.
Replication \code{r} uses \code{seed + r}, with fixed offsets for sampling and fitting.}
}
\value{
A list with \code{estimates} (including failed fits), \code{summary},
\code{settings} (including the seed), and \code{session_info}. Coverage and width
summaries use finite intervals; failure counts are reported separately.
}
\description{
Runs the paper's estimators on independently generated datasets. Gaussian
fits use three outer folds and twice-repeated three-fold tuning; the Sobolev
sensitivity uses two outer folds and twice-repeated two-fold tuning.
}
\examples{
\dontrun{
result <- pmtp_simulate(n = 750, beta_z = 0.5, beta_w = -0.5,
                        replications = 1:500, seed = 20280722)
result$summary
}
}
\seealso{
\code{\link[=pmtp_simulation_grid]{pmtp_simulation_grid()}}
}
